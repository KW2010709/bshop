<%= flash[:alert] %>
<h4><a href="#">UDM?IDU! 구매 후기</a> || <a href="/soju">소주</a> | <a href="/vodka">보드카</a> | <a href="/wine">와인</a> || <a href="/foods/write">글쓰기</a></h4>


<div>
  <h2>여러분의 구매 후기를 자유롭게 남겨주세요.</h2>
 
</div>

<hr>

<% @posts.reverse.each do |post| %>
<div style="border: 1px solid #ffffff">
  <h4><a href="/foods/show/<%= post.id %>"><%= post.title %></a>[<%= post.comments.count %>]</h4>
  <p style="color: #ff0000"><%= post.category %></p>
 <p><strong><%= post.user.username %></strong></p>

</div>
<% end %>
